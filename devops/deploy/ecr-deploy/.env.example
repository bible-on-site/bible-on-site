# AWS ECR Deployment Configuration (IAM Identity Center / SSO)
# Copy this file to .env and fill in your values

# AWS Region where ECR repositories will be created
# Example: us-east-1, eu-west-1, ap-southeast-1
AWS_REGION=us-east-1

# AWS SSO Profile name (configured via 'aws configure sso')
# This is the profile name you created during SSO setup
AWS_PROFILE=my-dev-profile

# (Optional) AWS Account ID - will be auto-detected from STS if not provided
# AWS_ACCOUNT_ID=123456789012
