<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:viewmodels="clr-namespace:BibleOnSite.ViewModels"
             xmlns:models="clr-namespace:BibleOnSite.Models"
             x:Class="BibleOnSite.Pages.ArticlesPage"
             x:DataType="viewmodels:ArticlesViewModel"
             Title="{Binding DisplayTitle}"
             FlowDirection="RightToLeft">

    <Grid RowDefinitions="Auto,*">
        <!-- Header -->
        <VerticalStackLayout Grid.Row="0"
                             Padding="16,12"
                             BackgroundColor="{AppThemeBinding Light={StaticResource Primary}, Dark={StaticResource PrimaryDark}}">
            <Label
                Text="{Binding DisplayTitle}"
                FontSize="20"
                FontAttributes="Bold"
                HorizontalTextAlignment="Center"
                TextColor="White"/>
            <Label
                Text="{Binding Articles.Count, StringFormat='{0} מאמרים'}"
                FontSize="14"
                HorizontalTextAlignment="Center"
                TextColor="White"
                Opacity="0.9"
                IsVisible="{Binding HasArticles}"/>
        </VerticalStackLayout>

        <!-- Articles Grid (Masonry-style with FlexLayout) -->
        <ScrollView Grid.Row="1" Margin="8,8,8,0">
            <FlexLayout BindableLayout.ItemsSource="{Binding Articles}"
                        Wrap="Wrap"
                        JustifyContent="Start"
                        AlignContent="Start"
                        AlignItems="Start"
                        Direction="Row">
                <BindableLayout.ItemTemplate>
                    <DataTemplate x:DataType="models:Article">
                        <Frame Padding="0"
                               Margin="4"
                               CornerRadius="12"
                               WidthRequest="180"
                               BackgroundColor="{AppThemeBinding Light={StaticResource Surface}, Dark={StaticResource SurfaceDark}}"
                               BorderColor="Transparent"
                               HasShadow="True">
                            <Frame.GestureRecognizers>
                                <TapGestureRecognizer Tapped="OnArticleTapped" CommandParameter="{Binding .}"/>
                            </Frame.GestureRecognizers>
                            <Grid RowDefinitions="Auto,Auto,Auto,Auto" Padding="12">
                                <!-- Article Name -->
                                <Label Grid.Row="0"
                                       Text="{Binding Name}"
                                       FontSize="14"
                                       FontAttributes="Bold"
                                       LineBreakMode="WordWrap"
                                       MaxLines="2"/>

                                <!-- Short Abstract -->
                                <Label Grid.Row="1"
                                       Text="{Binding ShortAbstract}"
                                       FontSize="12"
                                       TextColor="{AppThemeBinding Light={StaticResource Gray600}, Dark={StaticResource Gray300}}"
                                       LineBreakMode="TailTruncation"
                                       MaxLines="3"
                                       Margin="0,4,0,8"/>

                                <!-- Author Info (when viewing by perek) -->
                                <HorizontalStackLayout Grid.Row="2"
                                                       Spacing="6"
                                                       VerticalOptions="Center"
                                                       IsVisible="{Binding Author, Converter={StaticResource StringNotEmptyConverter}}">
                                    <Frame WidthRequest="24"
                                           HeightRequest="24"
                                           CornerRadius="12"
                                           Padding="0"
                                           IsClippedToBounds="True"
                                           BorderColor="{StaticResource Primary}">
                                        <Image Source="{Binding Author.ImageUrl}"
                                               Aspect="AspectFill"
                                               WidthRequest="24"
                                               HeightRequest="24"/>
                                    </Frame>
                                    <Label Text="{Binding Author.ShortenedName}"
                                           FontSize="11"
                                           VerticalTextAlignment="Center"
                                           TextColor="{StaticResource Primary}"/>
                                </HorizontalStackLayout>

                                <!-- Read indicator -->
                                <Label Grid.Row="3"
                                       Text="קרא עוד ◀"
                                       FontSize="11"
                                       HorizontalTextAlignment="End"
                                       TextColor="{StaticResource Primary}"
                                       Margin="0,4,0,0"/>
                            </Grid>
                        </Frame>
                    </DataTemplate>
                </BindableLayout.ItemTemplate>
            </FlexLayout>
        </ScrollView>

        <!-- Empty/Loading State Overlay -->
        <VerticalStackLayout Grid.Row="1"
                             VerticalOptions="Center"
                             HorizontalOptions="Center"
                             Padding="20"
                             IsVisible="{Binding HasArticles, Converter={StaticResource InvertedBoolConverter}}">
            <!-- Loading State -->
            <VerticalStackLayout IsVisible="{Binding IsLoading}">
                <ActivityIndicator IsRunning="{Binding IsLoading}"
                                   Margin="0,10,0,0"/>
                <Label Text="טוען מאמרים..."
                       FontSize="16"
                       HorizontalTextAlignment="Center"
                       Margin="0,10,0,0"/>
            </VerticalStackLayout>

            <!-- No Articles State -->
            <VerticalStackLayout IsVisible="{Binding IsLoading, Converter={StaticResource InvertedBoolConverter}}">
                <Label Text="📚"
                       FontSize="48"
                       HorizontalTextAlignment="Center"/>
                <Label Text="אין מאמרים"
                       FontSize="18"
                       HorizontalTextAlignment="Center"
                       Margin="0,10,0,0"/>
                <Label Text="{Binding ErrorMessage}"
                       FontSize="14"
                       TextColor="{StaticResource Error}"
                       HorizontalTextAlignment="Center"
                       IsVisible="{Binding ErrorMessage, Converter={StaticResource StringNotEmptyConverter}}"
                       Margin="0,10,0,0"/>
            </VerticalStackLayout>
        </VerticalStackLayout>
    </Grid>

</ContentPage>
