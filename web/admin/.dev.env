# Database connection
DB_URL=mysql://root:test_123@localhost:3306/tanah-dev

# Admin API URL (legacy - for reference)
VITE_API_URL=http://localhost:3004/api

# AWS S3 Configuration (server-side)
S3_REGION=il-central-1
S3_BUCKET=bible-on-site-assets-dev
S3_ACCESS_KEY_ID=test
S3_SECRET_ACCESS_KEY=test_1234

# MinIO S3 mock (comment out for real AWS)
S3_ENDPOINT=http://localhost:4566
S3_FORCE_PATH_STYLE=true

# Skip Cognito authentication for local development.
# To test with real auth, create a .env file (gitignored) with:
#   COGNITO_USER_POOL_ID, COGNITO_CLIENT_ID, COGNITO_CLIENT_SECRET, COGNITO_DOMAIN
SKIP_AUTH=true

# Website invalidation endpoint
VITE_WEBSITE_URL=http://localhost:3001
VITE_REVALIDATION_SECRET=dev-secret
