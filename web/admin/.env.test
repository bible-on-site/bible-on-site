# Test environment configuration

# Skip auth for E2E tests (auth is tested separately)
SKIP_AUTH=true

# Database connection (test database)
DB_URL=mysql://root:test_123@localhost:3306/tanah_test

# AWS S3 Configuration (server-side)
S3_REGION=us-east-1
S3_BUCKET=bible-on-site-assets-test
S3_ACCESS_KEY_ID=test
S3_SECRET_ACCESS_KEY=test_1234

# MinIO S3 mock
S3_ENDPOINT=http://localhost:4566
S3_FORCE_PATH_STYLE=true

# Website invalidation endpoint (not used in tests)
VITE_WEBSITE_URL=http://localhost:3001
VITE_REVALIDATION_SECRET=test-secret
